# Private content

Keep real private files here locally or in a private repository. They are ignored by Git by default.

The browser decrypts the generated private payload with the access code. This is useful for low-risk personal content, not for banking-grade secrecy.
